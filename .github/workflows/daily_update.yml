name: Update IPTV Channels

on:
  schedule:
    # প্রতিদিন বাংলাদেশ সময় সকাল ৬টার দিকে (UTC 00:00) আপডেট হবে
    - cron: '0 * * * *'
  workflow_dispatch: # ম্যানুয়ালি রান করার অপশন

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write # ফাইলে লেখার পারমিশন দেওয়া

    steps:
    - name: Checkout content
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests

    - name: Run Update Script
      run: python update_channels.py

    - name: Commit and Push changes
      run: |
        git config --global user.name "Auto-Updater"
        git config --global user.email "actions@github.com"
        git add .
        # যদি কোনো পরিবর্তন থাকে তবেই কমিট করবে
        git diff --quiet && git diff --staged --quiet || (git commit -m "🚀 Auto-updated channels list" && git push)

